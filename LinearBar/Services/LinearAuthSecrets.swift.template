import Foundation

/// Linear OAuth credentials
/// IMPORTANT: Copy this file to LinearAuthSecrets.swift and add your actual credentials
/// The .swift file is gitignored, this .template is committed for reference
struct LinearAuthSecrets {
    static let clientId = "YOUR_LINEAR_CLIENT_ID"
    static let clientSecret = "YOUR_LINEAR_CLIENT_SECRET"
}
