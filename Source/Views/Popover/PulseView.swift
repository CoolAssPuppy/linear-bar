import SwiftUI

/// Placeholder while the Pulse tab lands. Real implementation will render
/// the active cycle card + threatening-the-cycle issue list.
struct PulseView: View {
    var body: some View {
        EmptyStateView(icon: "waveform.path.ecg", title: "Pulse", subtitle: "Coming in the next milestone.")
    }
}
