import SwiftUI

/// Placeholder while the flat Recent tab lands. Real implementation will
/// render the touched-by-me activity query.
struct RecentView: View {
    var body: some View {
        RecentlyUpdatedView()
    }
}
